{
  "name": "testim-qase-test-run-reporter",
  "version": "1.0.0",
  "description": "Reporting test run results from Testim to the Qase TMS",
  "main": "app.js",
  "scripts": {
    "compile": "tsc -p ./tsconfig.json --noEmit",
    "start": "node ./src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Krisztian Gyula Toth",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "axios": "^1.7.8",
    "node-schedule": "^2.1.1",
    "pino": "^9.5.0",
    "testim-qase-test-run-reporter": "file:"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/node-schedule": "^2.1.7",
    "typescript": "^5.7.2"
  }
}
